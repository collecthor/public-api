type: object
title: Reward
properties:
  id:
    type: integer
  amount:
    type: integer
  member_id:
    type: integer
  reason:
    type: string
    description: Should contain a textual description such that the panel member can identify the tasks for which the reward was received.
  response_id:
    type:
      - integer
      - 'null'
    description: 'The id of a response, if this reward is related to a response.'
  created:
    type: string
  created_by:
    type: integer
required:
  - id
  - amount
  - member_id
  - reason
  - created
  - created_by
readOnly: true
